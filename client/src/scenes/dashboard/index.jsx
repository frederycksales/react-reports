import { Box, Typography } from "@mui/material";
import React from "react";
import Header from "../../components/Header";

const Dashboard = () => {
  return ( 
  <Box>
    <Header title="DASHBOARD" subtitle="Página do dashboard" />
  </Box>

  );
};

export default Dashboard;
